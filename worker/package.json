{
  "name": "statusline-community-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "test": "bun test",
    "seed": "bun run scripts/seed.ts"
  },
  "dependencies": {
    "@resvg/resvg-wasm": "^2.6.2",
    "@statusline/shared": "workspace:*",
    "nanoid": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
